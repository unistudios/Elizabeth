{% if object_list %}
{section}

{column:width=50%}
{center}
	{chart:title= Server installs }
	|| Total || Installed || Not Installed ||
	|| | {{nRunHostsInstalled}} | {{ nRunHostsRemaining }} ||
	{chart}
{center}
{column}

{column:width=50%}
{center}
	{chart:title= Users }
	|| Total || Disabled || Enabled ||
	|| | {{nRunUsersDisabled}} | {{nRunUsersEnabled}} ||
	{chart}
{center}
{column}

{section}
{align:right}{{dToday}}{align}

{section}
|| Date || Server || Installed || Delayed || Keys || Comment ||
{% for obj in object_list %}| {% ifchanged obj.hostsetting.installdate %}*{{obj.hostsetting.installdate}}*{% endifchanged %}| {{obj.name}} | {% if obj.hostsetting.installed %}(/){%else%}(x){% endif %} | {% if obj.hostsetting.delayed %}(/){%endif%} | {% if obj.hostsetting.sshkeys %}(/){%endif%} | {{ obj.comment }} |
{% endfor %}{% endif %}
{section}
