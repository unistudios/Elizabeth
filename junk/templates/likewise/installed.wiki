{% if object_list %}
h2. Total Installations {{ object_list.count }}
as of {{ dToday }}
|| Date || Server || Application || Level || Delayed || Comment ||
{% for obj in object_list %}| {% ifchanged obj.hostsetting.installdate %}*{{obj.hostsetting.installdate}}*{% endifchanged %}| {{obj.name}} | {{obj.app.name}} | {{obj.get_level_display}} | {{ obj.hostsetting.delayed}} | {{ obj.comment }}
{% endfor %}{% endif %}